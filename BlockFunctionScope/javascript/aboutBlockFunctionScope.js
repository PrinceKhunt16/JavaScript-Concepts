/*

What is block scope ?
This scope restricts the variable that is declared inside a specific block, from access by the outside of the block. The let & const keyword facilitates the variables to be block scoped.

What is function scope ?
Each function creates a new scope. Variables defined inside a function are not accessible (visible) from outside the function.  

Which types variable have block scope ?
let and const have block scope.

Which types variable have function scope ?
var have function scope.

*/